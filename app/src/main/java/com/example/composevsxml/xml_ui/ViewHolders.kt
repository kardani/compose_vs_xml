package com.example.composevsxml.xml_ui

import android.view.View
import androidx.recyclerview.widget.RecyclerView

class ListItemSmallViewHolder(itemView: View) : RecyclerView.ViewHolder(itemView) {
    // ViewHolder for text items
    // Define your views here
}

class ListItemLargeViewHolder(itemView: View) : RecyclerView.ViewHolder(itemView) {
    // ViewHolder for image items
    // Define your views here
}